You are planning literature research for a project that builds a configuration layer around LLM coding agents (instruction files, lifecycle hooks, skill libraries, sub-agent routing, stop gates that re-run tests) and measures whether it changes correctness, false claims of completion, cost and wall time. The files in this directory are the project's current findings (findings-*.md) and the list of topics already researched (done.txt, one slug per line).

Write ONE file, new-topics.tsv, in the current directory. Nothing else. Six lines, tab separated, three fields: slug (lowercase, hyphens), topic (one sentence stating the question), seed sources (two or three real, fetchable sources with arXiv id or venue and year, or an explicit search instruction). Every topic must be absent from done.txt, must bear on an open question or unmeasured claim you find in the findings, and must be answerable from published literature. Do not invent citations: fetch each seed with the webfetch tool before listing it, and if you cannot fetch it, replace it with a search instruction. No header line, no prose.
