You are an adversarial fact checker. The file report.md in this directory was written by another model. Your job is to try to break it. For EVERY numbered source entry in report.md: fetch the cited URL with webfetch (arXiv abstract https://arxiv.org/abs/<id>, or the HTML full text https://arxiv.org/html/<id> when a figure or table is cited), and check that (a) the paper exists with those authors and that title, (b) the specific number, quote or claim attributed to it actually appears in it, and (c) the section, table or figure reference is right. Write ONE file, verification.md, in this directory, nothing else. Structure: a table with columns entry, verdict, evidence; verdict is one of VERIFIED (exists and says what is claimed), MISREAD (exists but the number, quote or claim is wrong, misattributed or overstated; say what it actually says), UNREACHABLE (could not fetch after two tries; not a judgement). Below the table, a short section listing every MISREAD with the corrected statement, and a one-line count: N verified, N misread, N unreachable. Be strict: a paraphrase that changes a number by rounding is fine, a number that does not appear is MISREAD, a claim attributed to the wrong paper is MISREAD. Do not rewrite report.md. Do not stop until verification.md exists and covers every entry.
