# Verification of report.md sources

Adversarial check: every numbered entry was fetched (arXiv abstract page; arXiv HTML full text where a section, table or figure is cited), and each quoted number, quote, and section/table/figure reference was compared against the fetched text. All 13 URLs fetched successfully on the first try; none unreachable.

| Entry | Verdict | Evidence |
|---|---|---|
| 1 | VERIFIED | Paper exists: Chen, Zaharia, Zou, "FrugalGPT: How to Use Large Language Models While Reducing Cost and Improving Performance", arXiv 2305.05176. Abstract contains both quoted sentences verbatim ("match the performance of the best individual LLM (e.g. GPT-4) with up to 98% cost reduction or improve the accuracy over GPT-4 by 4% with the same cost"). Full text: Table 3 gives 98.3% (HEADLINES), 73.3% (OVERRULING), 59.2% (COQA) cost savings. Table 1 caption/Section 1: "$30 for OpenAI's GPT-4 but only $0.2 for GPT-J hosted by Textsynth" for 10M input tokens. Case study (Section 4): thresholds 0.96 (GPT-J stage) and 0.37 (J1-L stage) with a DistilBERT regression scorer. LLM diversity (Section 4, Figure 4 caption): "for about 6% of the data, GPT-4 makes a mistake but GPJ-J (or J-L or GPT-C) gives the right answer on HEADLINES"; 13% on COQA is GPT-3 over GPT-4 (report says GPT-3, text says "GPT-3 provides the correct answer" — match). Setup: 12 LLM APIs from 5 providers, datasets of 10,000 / 2,400 / 7,982 examples, cascade length 3, DistilBERT scorer, learned router list and thresholds, train/test splits. All attributed numbers and quotes appear. |
| 2 | VERIFIED | Paper exists: Ong, Almahairi, Wu, Chiang, Wu, Gonzalez, Kadous, Stoica, "RouteLLM: Learning to Route LLMs with Preference Data", arXiv 2406.18665 (v4, Feb 2025). GPT-4 vs Mixtral 8x7B routing, >2x cost reduction in abstract. Table 6: MT Bench 3.66x at 95% GPT-4 quality; MMLU 1.41x (92%); GSM8K 1.49x (87%). Table 2 note: "Routers trained only on D_arena perform poorly due to most questions being out-of-distribution, but dataset augmentation with D_gold is highly effective." Table 5: benchmark-dataset similarity scores. Table 4: SW ranking APGR improvement +56.6% (Claude 3 Opus/Sonnet) and +49.8% (Llama 3.1 70B/8B) without retraining. Section 5.5/Table 7: most expensive router (SW ranking) "adds an extra cost of no more than 0.4% as compared to GPT-4 generation". Table 1: MF router CPT(50%) on MT Bench 25.32% → 13.40% with D_judge (GPT-4-judge-augmented). Setup: 80k Arena battles, 65k pairwise comparisons across 64 models (Section 5), four router architectures, MMLU 14,042 / MT Bench 160 / GSM8K 1,319 questions. All check. |
| 3 | VERIFIED | Paper exists: Hu, Bieker, Li, Jiang, Keigwin, Ranganath, Keutzer, Upadhyay, "RouterBench: A Benchmark for Multi-LLM Routing System", arXiv 2403.12031. Contribution 3: "monetary costs of LLM services can routinely vary by factors of 2-5x for comparable levels of performance" (the HTML renders the math as "22-5×", i.e. 2–5×). Section 4.4/Figure 3: "the surprising observation that GPT-4 is seldom chosen" — report's paraphrase is accurate. Section 5.3: routers exceed Zero on MMLU and Winogrande, comparable on Hellaswag and GSM8K, underperform on ARC-Challenge and MBPP. Cascade quotes verbatim: "surpass both individual LLMs and the Zero router by a significant margin when the router's judge has an error rate of up to 0.1" and "deteriorate rapidly, particularly when the error rate exceeds 0.2". Setup: 405,467 samples, 11 models, 8 datasets including MBPP and GSM8K; RAG dataset of 800 user queries where routers send time-sensitive queries to online models (Section 5.4). All check. |
| 4 | VERIFIED | Paper exists: Ding, Mallick, Wang, Sim, Mukherjee, Ruhle, Lakshmanan, Awadallah, "Hybrid LLM: Cost-Efficient and Quality-Aware Query Routing", arXiv 2404.14618, ICLR 2024. Abstract: "up to 40% fewer calls to the large model, with no drop in response quality". Table 1: Llama-2 13b/GPT-3.5-turbo at 40% cost advantage: r_trans drop 2.9% (report says "probabilistic router plus label transformation" = r_trans, correct); at 20%: 0.8% (r_prob and r_trans both 0.8); same-family 7b/13b at 40%: 0.0% (r_trans). Section 4.2 large gap (FLAN-t5 800m / Llama-2 13b): r_trans "achieves 40% cost advantages with 10.3% quality drop", while r_det and r_prob "perform marginally better than the random routing baseline". Section 3.2: probabilistic labels estimated by sampling 10 responses per model per query. Setup: MixInstruct, 10k training examples × 10 responses per model, 5k validation and 5k test (matching MixInstruct splits), three gap regimes, test-time threshold tuning. All check. |
| 5 | VERIFIED | Paper exists: Aggarwal, Madaan, Anand, Potharaju, Mishra, Zhou, Gupta, Rajagopal, Kappaganthu, Yang, Upadhyay, Faruqui, Mausam, "AutoMix: Automatically Mixing Language Models", arXiv 2310.12963, NeurIPS 2024 (v5). Abstract: "reducing computational cost by over 50% for comparable performance", five models, five datasets. Entailment-framed few-shot self-verification (Section 4.1) consumed by a POMDP router (Section 4.2). Section 5.4: POMDP "identifies that lower confidence values correspond to such cases, and instead of routing to the LLM as other methods do, returns the SLM answer, saving cost" (qasper/unsolvable cases); "with just 50 training examples, AutoMix maintains 15% Delta-ibc and beats the baselines by more than absolute 20%"; "even for a cost ratio as low as 1:10, AutoMix begins to deliver good gains". Table 1: Δibc for AutoMix+T/+P vs FrugalGPT/HybridLLM across five datasets and three SLMs — AutoMix best on nearly all cells (one tie with FrugalGPT on qasper/llama2-13b; report's "for most model pairs" is accurate). Setup: QASPER, QuALITY, CoQA, MuTual, DIPLOMAT; gpt-3.5, llama2-13b, mistral-7b vs GPT-4; cost ratios 1:60–1:200. All check. |
| 6 | VERIFIED | Paper exists: Bhola, Krishnan, Mukunda NS, "Scrouting: Cost-Aware Routing of Coding Agents by Scouting the Repository First", arXiv 2608.04804 (SuperScout). Section 3.2: overlap/containment "0.941, 0.912, and 0.773 across the three benchmarks" (SWE-bench Verified, Multilingual, Pro per the replay description); "no learned router we tested exceeded the solve rate of always picking the strongest model; every observed gap fell within noise". Abstract/Table IV: SuperScout 159/266 vs best model (Claude Opus 4.6) 158/266, $0.230 vs $1.274 per solve under the official capped budget tier; abstract "the handoff rather than the routing decision carries the result"; no-router ablation (Kimi K2.5 + handoff) ties at 159 solves, $0.227 per solve. Section 4.2: "Calibration revealed that most reproduction claims are in fact false" (calibration run: 32% genuine, 56% demonstrably false; benchmark census: 20% genuine, 70% false). Section 7.1: "Kimi K2.5 solo resolves 149 of 266 tasks (56.02%) at $0.106 per task, dominating GPT-5.2 solo (139/266, 52.26%, $0.570 per task) on both axes". Setup: full 266-task Python census of SWE-bench Pro, 50-call turn cap and $2.00 per-attempt cap, four fixers, all-in accounting including the searcher's $1.13 GPU bill, paired calibration on 99 valid paired tasks of 100 fresh. All check. |
| 7 | VERIFIED | Paper exists: Lu, Fan, Chen, Chen, Wei, Liu, "Balancing Latency and Accuracy of Code Completion via Local-Cloud Model Cascading", arXiv 2603.05974, FSE 2026 (MCCom). Abstract: "reduces inference latency by up to 47.9% and cuts down LLM usage by an average of 46.3%, but also improves the exact match rate of the large model by an average of 8.9%" — matches verbatim. Section 3.2: confidence = average probability of the first N=3 generated tokens, threshold 0.8 (Section 4.3); escalation also on implicit user rejection (user keeps typing). Sections 3.3–3.4: rejected small-model output reused as speculative draft for the large model and as a weighted retrieval query. Setup: RepoEval line (1,600) and API (1,600) subsets plus new StmtEval with 999 statements from 93 post-2024 repositories, three 7B cloud models, greedy decoding, exact-match acceptance simulation. All check. |
| 8 | VERIFIED | Paper exists: Lee, "Most of the LLM Routing Gap Is Task Type", arXiv 2608.23023. Abstract: identical matrix run twice at temperature 0 flipped "5.37% of the 4,116 model-question pairs" (Section 3.1: 221 of 4,116 cells); 29 questions routable under the both-run rule; static task-type table "improves 21 of the 29"; static table "answers 262 of 294 questions at $3.33 per run, against the best single model's 245 at $7.69"; "All of this is fitted and scored on the same 294 questions with no holdout". Section 5/Table 5: five model identities hold the best-single-model title across the seven scoring variants plus three per-language strata (ten rules total) — the report's "five models hold the top title across seven scoring variants (abstract, Section 5)" matches the paper's own framing. Section 7.4: "No contamination control on the coding task type". Setup: 14×294 matrix, 8,232 keys (8,229 scored), 7 task types × 3 languages, one gateway (OpenRouter), billed-cost accounting. All check. |
| 9 | VERIFIED | Paper exists: Shihab, Al Ahsan, Swaqeeb, "Opportunity Is Not Realizability: Selection-Valid Diagnostics for Multi-LLM Routing", arXiv 2608.08265. Abstract: "selection-valid intervals certify a population oracle gap of 9.7–30.7 points on every task, yet the strongest deployable prompt router recovers only 7.5–14.4% of it, and the simultaneous interval for the best of eleven tested policies has lower limit zero throughout" — verbatim. Table 1: gaps .097 (ARC-Challenge), .280 (HellaSwag), .200 (GSM8K), .307 (MMLU); Table 3: recovered 14.4%, 9.3%, 7.5%, 7.6% in that order; Table 2: G_Π lower limit zero and H_Π lower limit positive on all four datasets. Setup: eight checkpoints (0.5B–7B) from six families, four benchmarks at 200–300 items plus 500-item code and factual-QA settings, 50/50 calibration/test splits, five-fold cross-fitting, Clopper–Pearson based selection-valid intervals. All check. |
| 10 | VERIFIED | Paper exists: Yang, Fan, "Signal or Noise? A Benchmark Study of Agent Skills in Web Development", arXiv 2608.23067. Abstract: "target Skill injection reduces mean Pass@2 by 1.3% to 4.2%, lowers task completion depth, and increases token cost by 72% to 394%, with gains in only 17% to 36% of Skill-project pairs"; "injection is most costly where the model already holds a strong prior"; "anti-pattern rules outperform example-heavy content". Table 2: easy-task ΔPass@2 CIs exclude zero on every model, −4.0 (GPT-5.1) to −10.7 (Qwen) pp. Table 3: length term −3.3 (Sonnet) and −3.5 (Qwen); content term −1.1 (GPT-5.1) and −1.4 (DeepSeek). Table 4: cross-model Pearson coefficients −0.08 to +0.12 (all |r| ≤ 0.12). Section 4.5/Table 5: anti-pattern slice +3.1 pp pooled (p=0.008, the only directionally reliable slice); examples null pooled (−0.7), hurt Sonnet −15.3 pp. Setup: 31 skills, 50 Web-Bench projects, 1,000 ordered tasks, four models, N=3 replicates, four matched conditions (C0–C3) including byte-matched irrelevant control and leave-one-out ablation, paired bootstrap intervals. All check. |
| 11 | VERIFIED | Paper exists: Li, Gao, Lakshmanan, "WISERouter: LLM Routing with Workload Budget Constraint", arXiv 2607.23765. Section 1: "WR-Offline improves average quality by 14% over the best baseline on SWE-Bench at the tightest budget"; "training over 5,900× faster than neural-network based baselines for each new budget setting"; Table 2: WR-Offline 1.50 s vs FrugalGPT 8.85×10^3 s. Section 6.2/Figure 4: "the static variant overshoots the workload budget by >5%" while WR-Offline maintains compliance without sacrificing reward. Section 6.2: "FrugalGPT fails entirely at the lowest budgets on both datasets, as its cascade policy requires invoking multiple models per query" (Table 1: FrugalGPT scores 0.00 at the tightest budgets). Section 6.3: WR-Online matches offline while reducing required training data by 90% on RouterBench. Setup: RouterBench 36,497 queries and SWE-Bench (500), 50:50 splits, K-means into J=16 contexts, six budget levels per dataset in Table 1 (RouterBench B=2.52–151.42; SWE-Bench B=232.72–659.44), five runs for stochastic baselines. One nuance: the report's "six to twelve budget levels per dataset" — the paper's Table 1 shows six per dataset (twelve columns total); the report's phrasing overstates granularity but the total is twelve, and the load-bearing numbers (1.50 s vs 8.85e3 s, >5% overshoot, 14%, 90%) are exact. Counted as verified on the claims cited with quotes. |
| 12 | VERIFIED | Paper exists: Canedo, "Architecture as Capability Equalizer for Coding Agents", arXiv 2608.21747. Abstract: "On the strongest models (Sonnet 4.6, GPT-5), format barely matters (quality spread 0.17-0.92). On weaker models, format produces spreads of 0.83-2.42 points, with code-proximate formats (OpenAPI, TypeScript contracts) recovering most of the capability gap"; "TypeScript contracts triple API route coverage for the weakest model (33% to 100%)"; "Self-validation rates collapse from 100% (Sonnet) to 0% (Gemini Flash)"; "Mid-tier models can consume more tokens than frontier models for worse output when they enter compilation debugging loops that stronger models avoid". Setup: 90 multi-turn agent trials, five specification formats, six models from three vendor families. All claims verified at abstract level (as the report itself flags; full text not fetched). |
| 13 | VERIFIED | Paper exists: Valkanas, Pal, Rumiantsev, Zhang, Coates, "C3PO: Optimized Large Language Model Cascades with Probabilistic Cost Constraints for Reasoning", arXiv 2511.07396. Abstract: "It leverages conformal prediction to bound the probability that inference cost exceeds a user-specified budget"; "our optimization procedure is effective even with small calibration sets"; "C3PO achieves state-of-the-art performance across a diverse set of reasoning benchmarks including GSM8K, MATH-500, BigBench-Hard and AIME, outperforming strong LLM cascading baselines". Setup: self-supervised optimization minimizing regret w.r.t. the most powerful model (MPM), conformal cost bounds, four reasoning benchmarks. All claims verified at abstract level (as the report itself flags). |

## Corrections for MISREAD entries

None. No entry was judged MISREAD.

One nuance, not rising to MISREAD (entry 11): the report's "six to twelve budget levels per dataset" in the method line does not match the paper's Table 1, which uses exactly six budget levels per dataset (six on RouterBench and six on SWE-Bench, twelve columns total). Every quoted number and section reference for that entry is exact; the nuance affects only the setup paraphrase.

## Count

13 verified, 0 misread, 0 unreachable.
