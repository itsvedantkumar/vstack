# <project>

## Stack
<language / framework / package manager>

## Commands
- install: `<cmd>`
- dev: `<cmd>`
- test: `<cmd>`
- typecheck/lint: `<cmd>`

## Conventions
- Target branch for diffs and PRs: `origin/main` (`git diff origin/main...HEAD`).
- Agent scratch space is `.context/` (kept untracked via .git/info/exclude).
- Verify before claiming done: typecheck -> lint -> test.

## Gotchas
<things that will bite an agent: generated files, required env vars, slow steps>
