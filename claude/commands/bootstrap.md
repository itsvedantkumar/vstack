---
description: Scaffold founder-grade Claude Code + Conductor config into the current repo (idempotent)
---
Run `~/.claude/scripts/bootstrap-claude-project.sh "$PWD"`, then show the created CLAUDE.md and .conductor/settings.toml and point out any `# TODO` placeholders to fill.
