{
  "description": "Routes situations to skills so they fire without a slash command, and holds the agent to the routing it was given. The .claude/verify.sh Stop gate is NOT in this lane: it refuses to run an untrusted gate and tells the operator to run `vstack trust`, a command only the full install provides. A hook whose only outcome is naming a command you do not have is not a feature of this lane.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "VSTACK_PROFILE=skills \"${CLAUDE_PLUGIN_ROOT}/hooks/inject-session-context.sh\"",
            "statusMessage": "skill routing"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/skill-mandate.sh\""
          }
        ]
      }
    ]
  }
}
